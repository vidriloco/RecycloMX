<% if !flash[:notice].blank? || content_for?(:notice) %>
<div class="alert-container">
	<div class="alert alert-dismissible alert-success">
		<button class="close" data-dismiss="alert" type="button">×</button>
		<p class="centered"><%= flash[:notice]  || yield(:notice).html_safe %></p>
	</div>
</div>
<% end %>

<% if !flash[:warning].blank? || content_for?(:warning) %>
<div class="alert-container">
	<div class="alert alert-dismissible alert-warning">
		<button class="close" data-dismiss="alert" type="button">×</button>
		<p class="centered"><%= flash[:warning] || yield(:warning).html_safe %></p>
	</div>
</div>
<% end %>

<% if !flash[:error].blank?  || content_for?(:error) %>
<div class="alert-container">
	<div class="alert alert-dismissible alert-danger">
		<button class="close" data-dismiss="alert" type="button">×</button>
		<p class="centered"><%= flash[:error] || yield(:error).html_safe %></p>
	</div>
</div>
<% end %>